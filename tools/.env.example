# Plejd cloud credentials, used only by the reverse-engineering tools to fetch
# the site crypto key + device list. Copy to tools/.env (gitignored) and never
# commit real values.
PLEJD_USER=you@example.com
PLEJD_PASS=
